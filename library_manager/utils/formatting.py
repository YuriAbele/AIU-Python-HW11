def format_book_data(data: dict) -> str:
    """
    Исключительно для демонстрации импорта поледующего импорта этой функции.
    Правильно же, когда объект сам себя описывает (ООП!)
    """
    return f"Title: {data["title"]}, Author: {data["author"]}, Genre: {data["genre"]}"
